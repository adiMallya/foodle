const breakpoints = {
    sm: '512px',
    md: '768px',
    lg: '1024px',
};

export const device = {
    sm: `(min-width: ${breakpoints.sm})`,
    md: `(min-width: ${breakpoints.md})`,
    lg: `(min-width: ${breakpoints.lg})`
};
